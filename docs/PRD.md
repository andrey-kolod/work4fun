# 🚀 Work4Fun — Product Requirements Document (PRD)

**Версия:** 1.0 (MVP)  
**Дата:** 15 декабря 2025  
**Автор:** Андрей К **Репозиторий:**
[https://github.com/andrey-kolod/work4fun](https://github.com/andrey-kolod/work4fun)  
**Статус:** ✅ Активно разрабатывается

---

## 📖 Оглавление

1. [Введение](#1-введение)
2. [Пользователи и роли](#2-пользователи-и-роли)
3. [Функциональные требования](#3-функциональные-требования)
4. [Нефункциональные требования](#4-нефункциональные-требования)

---

## 1. Введение

### 1.1 Цель документа

Этот PRD определяет полные требования к системе управления задачами **Work4Fun версии 1.0 (MVP)**.
Документ служит единым источником истины для команды разработки, тестировщиков и стэкхолдеров.

### 1.2 Обзор продукта

Work4Fun — веб-приложение на Next.js для управления задачами в малых и средних командах (до 50
пользователей). Ключевые особенности:

- ✅ **Три роли**: Супер-Админ, Руководитель, Сотрудник
- ✅ **Канбан-доска**: Drag-and-drop задачи по статусам
- ✅ **Умные уведомления**: In-app, email, Telegram (админ управляет получателями)
- ✅ **Делегирование**: Передача задач между сотрудниками
- ✅ **Ограничения**: Максимум 3 проекта на обычного пользователя
- ✅ **Вся история**: Логирование всех действий, история статусов

### 1.3 Область применения (MVP v1.0)

| ✅ Включено в MVP v1.0                          | ❌ Отложено до v2.0+                              |
| :---------------------------------------------- | :------------------------------------------------ |
| Аутентификация и регистрация                    | Ночная тема                                       |
| Управление проектами, группами, пользователями  | Нативное мобильное приложение                     |
| Канбан-доска с фильтрацией по `scope`           | Интеграция с календарями (Google Calendar и т.д.) |
| Уведомления (in-app, email, Telegram)           | Расширенная аналитика и BI                        |
| Комментарии и @упоминания                       | Workflow-автоматизация                            |
| Дашборд, персонализированный по ролям           | Полноценный чат с комнатами                       |
| Полное логирование действий (ActivityLog)       | Расширенная система отчетов                       |
| Управление всеми процессами через веб-интерфейс | Перевод интерфейса на английский                  |
| Профиль пользователя                            | График работы сотрудников                         |

## 2. Пользователи и роли

### 2.1 Роли

```
graph TD
    SA[Супер-Админ] -->|Полный контроль| System[Вся система]
    A[Руководитель] -->|Управление| Project[Свой проект]
    U[Сотрудник] -->|Работа с| Tasks[Назначенные задачи]
```

### 2.2 Детализация прав и ограничений

#### 2.2.1 Права

```
### 2.2 Роли в системе

| Роль в системе | Техническое название | Описание |
|----------------|----------------------|----------|
| **СИСТЕМНЫЙ АДМИН** | `SUPER_ADMIN` | Полный доступ ко всей системе, настройки, мониторинг |
| **РУКОВОДИТЕЛЬ** | `PROJECT_LEAD` | Может создавать проекты (≤3), полное управление своими проектами |
| **СОТРУДНИК** | `PROJECT_MEMBER` | Только работа с задачами в назначенных проектах |

#### 2.2.1 Детализация прав

────────────────────────────────────────────
1. СИСТЕМНЫЙ АДМИН (SUPER_ADMIN)
────────────────────────────────────────────
• ✅ Полный доступ ко всем проектам и пользователям
• ✅ Настройки системы (SMTP, домены, интеграции)
• ✅ Просмотр всех системных логов
• ✅ Назначение/снятие роли "Руководитель"
• ✅ Изменение лимитов проектов для пользователей
• ✅ Полное управление всеми сущностями: пользователями, проектами, задачами (деактивация/активация/удаление)

Ограничения: НЕТ (полный контроль)

────────────────────────────────────────────
2. РУКОВОДИТЕЛЬ (PROJECT_LEAD)
────────────────────────────────────────────
Права в СВОИХ проектах:
• ✅ Создание проектов (макс. 3 активных)
• ✅ Приглашение пользователей в свои проекты
• ✅ Назначение ролей "Руководитель" или "Сотрудник" в своих проектах
• ✅ Управление группами и задачами в своих проектах
• ✅ Управление участниками проекта: деактивация/активация/удаление из проекта
• ✅ Просмотр аналитики своих проектов

Ограничения:
• ❌ Не видит чужие проекты
• ❌ Не имеет доступа к системным настройкам
• ❌ Максимум 3 активных проекта

────────────────────────────────────────────
3. СОТРУДНИК (PROJECT_MEMBER)
────────────────────────────────────────────
Права в назначенных проектах:
• ✅ Просмотр своих задач и задач группы
• ✅ Изменение статуса своих задач
• ✅ Делегирование своих задач
• ✅ Комментирование задач

Запрещено:
• ❌ Создание проектов
• ❌ Приглашение других пользователей
• ❌ Создание/удаление групп
• ❌ Доступ к аналитике проекта
```

#### 2.2.2 Как становятся Руководителем

```
1. Назначение Системным Админом (глобально)
   - Системный Админ через админ-панель назначает роль "Руководитель"
   - Пользователь сразу получает право создавать проекты

2. Приглашение в проект как Руководитель
   - Другой Руководитель приглашает в свой проект с ролью "Руководитель"
   - Пользователь получает право создавать проекты (если ещё не Руководитель)
   - Лимит проектов: 3 (проверяется при создании нового проекта)

3. Самостоятельная регистрация → НЕТ
   - При самостоятельной регистрации пользователь становится "Сотрудником"
   - Чтобы стать Руководителем, нужно получить приглашение или назначение
```

#### 2.2.3 Особые случаи

```
• Разные роли в разных проектах:
    Пользователь может быть Руководителем в Проекте А, но Сотрудником в Проекте Б.

• Лимит 3 проекта для Руководителя:
    Считаются только проекты, где пользователь является создателем или Руководителем.
    Если Руководитель в 2 проектах и Сотрудник в 5 других → лимит: 2/3.

• Удаление vs Деактивация:
    Руководитель может деактивировать/активировать и удалять пользователей из своего проекта.
    Системный Админ может удалять пользователей из системы полностью.
    Пользователь может удалить свой аккаунт с периодом ожидания.
```

#### 2.2.4 Начальные роли при регистрации

```
1. **Самостоятельная регистрация** (/register):
   • Роль в системе: **Сотрудник** (PROJECT_MEMBER)
   • Не состоит ни в одном проекте
   • Не может создавать проекты
   • Может быть приглашён в проекты как Сотрудник или Руководитель

2. **Регистрация по приглашению:**
   • Роль в системе: **Сотрудник** (базовая)
   • Роль в проекте: указана в приглашении ("Руководитель" или "Сотрудник")
   • Если приглашён как "Руководитель" → получает глобальную роль Руководителя
   • Может создавать проекты (с учётом лимита 3)

3. **Назначение Системным Админом:**
   • Системный Админ может изменить роль на "Руководитель"
   • Пользователь сразу получает право создавать проекты
   • Лимит проектов: 3 (по умолчанию)
```

## 3. Функциональные требования

Почта проекта w4fteam@gmail.com

```
На главной странице системы (/) отображаются:
    - Логотип "Work4Fun";
    - Кнопка "Вход" (/login);
    - Кнопка "Регистрация" (/register); (скрыта до запуска в продакшене)
    - Кнопка "Запросить демо (/demo)".

```

### 3.1 🔐 Авторизация и регистрация

#### 3.1.1 Регистрация пользователя в системе через форму регистрации

```
Пользователь имеет возможность перейти на форму регистрации нажатием на кнопку "Регистрация" на главной странице системы (/) или по прямой ссылке (/register).

Форма регистрации содержит следующие поля:

    - "Электронная почта" (обязательно):
        - Уникальный в системе;
        - Валидный формат электронной почты;
        - Максимум 255 символов.

    - "Имя" (обязательно):
        - Принимает буквы латинского и кириллического алфавитов, пробелы, дефисы и апострофы;
        - Принимает от 2 до 50 символов;
        - Автоматическое обрезание пробелов в начале и конце строки;
        - Валидация при вводе:
            - Если введены невалидные символы, поле подсвечивается красным цветом и пользователь видит сообщение: "Введены невалидные символы";
            - Если поле пустое, содержит только пробелы или менее 2 символов, поле подсвечивается красным цветом и пользователь видит сообщение: "Должно быть от 2 до 50 символов".

    - "Фамилия" (обязательно):
        - Принимает буквы латинского и кириллического алфавитов, пробелы, дефисы и апострофы;
        - Принимает от 2 до 50 символов;
        - Автоматическое обрезание пробелов в начале и конце строки;
        - Валидация при вводе:
            - Если введены невалидные символы, поле подсвечивается красным цветом и пользователь видит сообщение: "Введены невалидные символы";
            - Если поле пустое, содержит только пробелы или менее 2 символов, поле подсвечивается красным цветом и пользователь видит сообщение: "Должно быть от 2 до 50 символов".

    - "Телефон" (не обязательно):
        - Текстовое поле;
        - Принимает только цифры, скобки, пробелы, плюс.

    - "Пароль" (обязательно):
        - Пароль должен содержать минимум 8 символов;
        - Пароль должен содержать хотя бы одну букву латинского алфавита в верхнем и нижнем регистре;
        - Пароль должен содержать хотя бы одну цифру;
        - Независимая кнопка "Показать/скрыть пароль". По умолчанию скрыт;
        - Валидация требований при вводе. Если пароль не соответствует требованиям, поле подсвечивается красным цветом и пользователь видит сообщение: "Пароль должен содержать цифры и буквы латинского алфавита в верхнем и нижнем регистре".

    - "Подтвердить пароль" (обязательно):
        - Поле обязательно для заполнения;
        - Пароли должны совпадать.
        - Валидация требований при вводе. Если пароли не совпадают, поле подсвечивается красным цветом и пользователь видит сообщение: "Пароли не совпадают";
        - Независимая кнопка "Показать/скрыть пароль". По умолчанию скрыт.

    - "Пользовательское соглашение" (обязательно):
        - Чекбокс. Не активен по умолчанию;
        - Текст: "Я согласен с пользовательским соглашением";
        - Ссылка на пользовательское соглашение (/terms).

    - "Политика конфиденциальности" (обязательно):
        - Чекбокс. Не активен по умолчанию;
        - Текст: "Я согласен с политикой конфиденциальности";
        - Ссылка на политику конфиденциальности (/privacy-policy).

    - "Капча" (обязательно):
        - Компонент Google reCAPTCHA v3 или hCaptcha;
        - Виджет должен быть успешно решен пользователем для активации кнопки отправки формы.

    - Кнопка "Зарегистрироваться":
        - Изначально неактивна.
        - Становится активной только после:
            1. Прохождения базовой клиентской валидации всех обязательных полей (формат, длина).
            2. Отметки обоих обязательных чекбоксов.
            3. Успешного решения капчи.

Пользователь нажимает кнопку "Зарегистрироваться":
    - Проверка капчи: Если капча не была решена или решена неверно, отправка блокируется.
    - Отправка данных на сервер (`/api/auth/register`).
    - Серверная обработка:
        1. Валидация капчи (интеграция с сервисом, например, Google reCAPTCHA).
        2. Rate limiting: Проверка IP-адреса (макс. 100 запросов в минуту).
        3. Валидация данных: Повторная проверка форматов.
        4. Нормализация телефона: Если номер введен, он приводится к формату E.164 (только `+` и цифры) для хранения.
        5. Проверка уникальности `email` в базе данных.

    При успешном завершении регистрации:
        - Создается пользователь в базе данных с 'isEmailVerified = false';
        - Пользователю направляется письмо с ссылкой подтверждения регистрации;
        - Пользователь видит сообщение "Письмо с подтверждением регистрации отправлено на вашу электронную почту";
        - Пользователь переходит по ссылке подтверждения регистрации и перенаправляется на страницу авторизации (/login).

    При неудачном завершении регистрации пользователь получает сообщение об ошибке:
        - если почта уже зарегистрирована:
            - "Пользователь с такой электронной почтой уже зарегистрирован. Попробуйте восстановить пароль.":
            - На форме отображается подсвеченная ссылка "Восстановить пароль" (/password/reset);
            - Поле "Почта" подсвечивается красным цветом;
            - Введенный текст сохраняется в поле "Почта".
        - если email невалиден:
            - "Невалидная электронная почта. Попробуйте ещё раз.":
            - Поле "Почта" подсвечивается красным цветом;
            - Защита от атак на сервере ✅ Упростить: Rate limiting на уровне API (100 запросов/минуту на IP);
            - Введенный текст сохраняется в поле "Почта".
        - если более 100 запросов/минуту с одного IP:
            - "Слишком много запросов. Попробуйте через минуту.";
            - Отображается счетчик обратного отсчета времени на 60 секунд;
            - Кнопка "Зарегистрироваться" до завершения отсчета времени не активна.
        - если ошибка на сервере:
            - "Произошла ошибка на сервере. Попробуйте ещё раз.";
            - Кнопка "Зарегистрироваться" активна;
            - Сообщение об ошибке отображается ниже формы.

* Письмо с подтверждением регистрации:
    - Логотип Work4Fun;
    - Текст: "Для завершения регистрации подтвердите вашу электронную почту, нажав на кнопку ниже.";
    - Кнопка "Подтвердить email" (ведет на `/auth/verify?token=...`);
    - Примечание: "Если вы не создавали учетную запись, проигнорируйте это письмо.";
    - Контакты поддержки.

При отсутствии подтверждения регистрации пользователь будет удален из базы данных через 24 часа.

```

#### 3.1.2 Запрос демо-доступа

```
Форма запроса демо-доступа отображается на главной странице системы (/) при нажатии на кнопку "Запросить демо".

Форма запроса демо содержит следующие поля:

    - "Ваше имя" (обязательно):
        - Принимает буквы латинского и кириллического алфавитов, пробелы, дефисы и апострофы;
        - Принимает от 2 до 50 символов.

    - "Электронная почта для связи" (обязательно):
        - Валидный формат электронной почты;
        - Максимум 255 символов.

    - "Название компании" (не обязательно):
        - Текстовое поле.
        - Максимум 255 символов.

    - "Сообщение или цель запроса" (не обязательно):
        - Текстовое поле.
        - Максимум 500 символов.

    - "Политика конфиденциальности" (обязательно):
        - Чекбокс. Не активен по умолчанию;
        - Текст: "Я согласен на обработку моих данных в соответствии с политикой конфиденциальности";
        - Ссылка на политику конфиденциальности (/privacy-policy).

    - Кнопка "Отправить запрос":
        - Изначально неактивна.
        - Становится активной только после:
            1. Заполнения всех обязательных полей.
            2. Отметки обязательного чекбокса.

Пользователь нажимает кнопку "Отправить запрос":
    - Отправка данных на сервер (`/api/demo-request`).
    - Серверная обработка:
        1. Rate limiting: Проверка IP-адреса (макс. 10 запросов в минуту).
        2. Валидация данных: Проверка форматов.

    При успешной отправке:
        - Запись о запросе сохраняется в административной панели (админка).
        - Администратор системы получает уведомление о новом запросе (email/telegram).
        - Заявка помечается статусом "Новая".
        - Пользователь видит сообщение: "Спасибо за интерес! Мы свяжемся с вами в ближайшее время по указанной почте."

    При неудачной отправке пользователь получает сообщение об ошибке:
        - если email невалиден:
            - "Невалидная электронная почта. Попробуйте ещё раз.";
            - Поле "Электронная почта" подсвечивается красным цветом.
        - если более 10 запросов/минуту с одного IP:
            - "Слишком много запросов. Попробуйте позже.";
        - если ошибка на сервере:
            - "Произошла ошибка. Попробуйте ещё раз позже."

Дальнейший процесс (вне формы):
Администратор связывается с пользователем по указанному email.
В случае положительного решения, администратор вручную создает пользователя или проект через панель администрирования.
```

#### 3.1.3 Регистрация пользователя в системе через приглашение

```
Владелец проекта (администратор проекта) или супер-админ приглашает пользователя в систему через форму создания пользователя (/admin/users/create) или через форму приглашения в проект.

Администратор заполняет поля:
    - "Email" (обязательно): email пользователя
    - "Роль в проекте" (обязательно): выбор из списка:
        * "Руководитель" — полный доступ к управлению проектом, даёт право создавать проекты
        * "Сотрудник" — базовые права работы в проекте
    - "Проект" (обязательно): выбор проекта
    - "Имя" и "Фамилия" (необязательно)

Нажимает кнопку "Пригласить" или "Создать".

Происходит проверка на сервере:

    - Если email пользователя НЕТ в системе:
        1. Создается запись пользователя в базе данных с полями:
            - `isEmailVerified = false`
            - `isActive = true`
            - `hasPasswordSet = false` (флаг, что пароль ещё не установлен)
        2. Пользователь привязывается к указанному проекту с выбранной ролью
        3. Генерируется уникальный токен для установки пароля (срок действия: 7 дней)
        4. Пользователю отправляется письмо с приглашением

        Письмо для нового пользователя (приглашение):
            - Логотип Work4Fun
            - Текст: "Вас приглашает {adminName} в систему Work4Fun для работы в проекте «{projectName}»."
            - Текст: "Для завершения регистрации нажмите кнопку ниже и установите свой пароль."
            - Кнопка "Войти и установить пароль" (ведёт на `/auth/set-password?token=...`)
            - Важное примечание: "Ссылка действительна 7 дней. После установки пароля ваш email будет автоматически подтверждён."
            - Ссылки на документы: "Пользовательское соглашение" (/terms) и "Политика конфиденциальности" (/privacy-policy)
            - Примечание: "Если вы не ожидали этого приглашения, проигнорируйте письмо."
            - Контакты поддержки: w4fteam@gmail.com

    - Если email пользователя УЖЕ ЕСТЬ в системе:
        1. Проверяется, не состоит ли пользователь уже в этом проекте
        2. Если приглашают как "Руководитель", а пользователь ещё не имеет этой роли:
            - Пользователю присваивается глобальная роль "Руководитель";
            - Теперь он может создавать свои проекты (с учётом лимита 3).
        3. Пользователю отправляется письмо-уведомление о приглашении в новый проект

        Письмо для существующего пользователя:
            - Логотип Work4Fun
            - Текст: "{adminName} приглашает Вас присоединиться к проекту «{projectName}» в системе Work4Fun."
            - Кнопка "Перейти в проект":
                * Для неавторизованного пользователя ведёт на `/login?redirect=/projects/{projectId}`
                * Для авторизованного — напрямую на страницу проекта `/projects/{projectId}`
            - Примечание: "Если вы не ожидали этого приглашения, проигнорируйте письмо."
            - Контакты поддержки: w4fteam@gmail.com

Сроки действия:
    - Для новых пользователей: если пароль не установлен в течение 7 дней — приглашение считается просроченным, запись пользователя деактивируется (`isActive = false`)
    - Для существующих пользователей: приглашение в проект действительно 7 дней. Если не принято (пользователь не зашёл в проект) — приглашение отзывается

Rate limiting: максимум 50 приглашений в час с одного IP и/или одного аккаунта администратора.
```

#### 3.1.4 Установка пароля по приглашению

```
Пользователь переходит по ссылке из письма-приглашения на страницу `/auth/set-password?token=...`.

Страница содержит:
    - Информацию о приглашении: "Вас пригласил {adminName} в проект «{projectName}»"
    - Поле "Новый пароль" (обязательно):
        - Минимум 8 символов, хотя бы одна заглавная буква, одна строчная буква, одна цифра (только латинский алфавит);
        - Независимая кнопка "Показать/скрыть пароль".
    - Поле "Подтверждение пароля" (обязательно):
        - Совпадает с полем "Новый пароль";
        - Независимая кнопка "Показать/скрыть пароль".
    - Кнопка "Установить пароль и войти"

При нажатии кнопки:
    1. Проверяется валидность токена (не просрочен, не использован ранее)
    2. Валидируется пароль по требованиям
    3. Если всё успешно:
        - Устанавливается хэш пароля в базе данных
        - Устанавливается `isEmailVerified = true`
        - Устанавливается `hasPasswordSet = true`
        - Токен помечается как использованный
        - Создаётся сессия (JWT в httpOnly cookie)
        - Пользователь перенаправляется на страницу проекта, в который его пригласили

Если токен невалиден или просрочен:
    - Показывается сообщение: "Ссылка недействительна или просрочена. Обратитесь к администратору для повторного приглашения."
    - Кнопка "Запросить новое приглашение" (отправляет email администратору проекта)
```

#### 3.1.5 Авторизация пользователя в системе

```
Для авторизации используется форма входа на странице `/login`.

Форма содержит:
    - "Электронная почта" (обязательно)
    - "Пароль" (обязательно) с кнопкой показа/скрытия
    - Чекбокс "Запомнить меня" (неактивен по умолчанию):
        * Если отмечен — сессия на 30 дней
        * Если не отмечен — сессия на 24 часа
    - Ссылка "Забыли пароль?" (/password/reset)
    - Кнопка "Войти" (активна после заполнения полей)

Пользователь нажимает "Войти":
    - Отправка на `/api/auth/login`
    - Серверная обработка:
        1. Rate limiting: 10 попыток/минуту с IP, 5 попыток на email
        2. Проверка существования пользователя
        3. Проверка `isActive = true` (если false — ошибка "Аккаунт деактивирован")
        4. Проверка `isEmailVerified = true` (если false — ошибка)
        5. Проверка `hasPasswordSet = true` (если false — перенаправление на установку пароля)
        6. Сравнение пароля

    При успешном входе:
    - Создаётся JWT-токен (в httpOnly cookie)
    - Пользователь перенаправляется:
        * Системный Админ: на панель управления системой (`/admin`)
        * Руководитель (есть проекты): выбор проекта (/projects)
        * Руководитель (нет проектов): предложение создать первый проект
        * Сотрудник (есть проекты): выбор проекта (/projects)
        * Сотрудник (нет проектов): "Вы пока не состоите ни в одном проекте"

    При неудачном входе:
        - Email не найден: "Неверный email или пароль."
        - Аккаунт неактивен (`isActive = false`):
            "Ваш аккаунт деактивирован. Обратитесь в поддержку для восстановления доступа."
        - Email не подтверждён (`isEmailVerified = false`):
            * Если `hasPasswordSet = false` (приглашённый пользователь):
                "Завершите регистрацию, установив пароль по ссылке из письма-приглашения."
            * Если `hasPasswordSet = true` (самостоятельно зарегистрировавшийся):
                "Электронная почта не подтверждена. Проверьте почту и перейдите по ссылке подтверждения."
        - Пароль не установлен (`hasPasswordSet = false`): автоматическое перенаправление на `/auth/set-password?token=...` (если есть активное приглашение)
        - Неверный пароль: "Неверный email или пароль." + счётчик попыток
        - Превышен лимит: "Слишком много попыток. Попробуйте через X минут."

Дополнительно:
    - После 5 неудачных попыток — уведомление на email
    - Все попытки входа логируются
    - Супер-админ видит логи всех пользователей
```

#### 3.1.6 Выбор проекта после входа

```
#### 3.1.6 Выбор проекта после входа

После успешной авторизации пользователь перенаправляется:

    Если пользователь состоит только в ОДНОМ проекте:
        - Автоматический переход на страницу этого проекта (/projects/{projectId})

    Если пользователь состоит в НЕСКОЛЬКИХ проектах:
        - Переход на страницу выбора проекта (/projects)
        - Отображаются все проекты, где пользователь является участником
        - Для каждого проекта показывается:
            * Название проекта
            * Роль пользователя в проекте (Руководитель/Сотрудник)
            * Количество активных задач
            * Дата последней активности

   Если пользователь НЕ СОСТОИТ ни в одном проекте:
        - Системный Админ: перенаправление на админ-панель (/admin)
        - Руководитель: кнопка "Создать первый проект" (лимит 1/3)
        - Сотрудник: сообщение "Вы пока не состоите ни в одном проекте. Ожидайте приглашения или обратитесь к Руководителю."

```

#### 3.1.7 Восстановление пароля

```
Пользователь может восстановить пароль через ссылку "Забыли пароль?" на странице входа (/login).

Процесс восстановления:

1. Запрос восстановления:
   - Пользователь переходит на `/password/reset`
   - Вводит email в поле "Электронная почта" (обязательно):
     - Валидный формат электронной почты
     - Максимум 255 символов
     - Клиентская валидация: если поле пустое или невалидный формат - подсветка красным и сообщение "Введите корректный email"
   - Нажимает кнопку "Отправить ссылку восстановления"

2. Проверка на сервере:
   - Rate limiting: максимум 5 запросов в час с одного IP, максимум 3 запроса на один email
   - Проверка существования пользователя с таким email
   - Проверка статусов:
     - Если `isActive = false`: возвращается сообщение "Аккаунт деактивирован. Обратитесь к Системному Админу"
     - Если `isEmailVerified = false`: возвращается сообщение "Email не подтверждён. Сначала подтвердите email"
     - Если `hasPasswordSet = false` (приглашённый пользователь): возвращается сообщение "Пароль ещё не установлен. Используйте ссылку из приглашения"
   - Если все проверки пройдены:
     - Генерация уникального токена восстановления (срок: 1 час)
     - Сохранение токена в базе данных с флагом `isUsed = false`
     - Отправка письма восстановления

3. Отправка письма:
   - Письмо содержит:
     - Логотип Work4Fun
     - Текст: "Вы запросили восстановление пароля для аккаунта в системе Work4Fun"
     - Кнопка "Восстановить пароль" (ведёт на `/password/reset/confirm?token=...`)
     - Примечание: "Ссылка действительна 1 час. Если вы не запрашивали восстановление пароля, проигнорируйте это письмо"
     - Контакты поддержки

4. Установка нового пароля:
   - Пользователь переходит по ссылке из письма на `/password/reset/confirm?token=...`
   - Страница содержит:
     - Информацию: "Установка нового пароля для [email пользователя]"
     - Поле "Новый пароль" (обязательно):
       - Минимум 8 символов
       - Должен содержать хотя бы одну букву латинского алфавита в верхнем и нижнем регистре
       - Должен содержать хотя бы одну цифру
       - Независимая кнопка "Показать/скрыть пароль"
       - Клиентская валидация: если требования не выполнены - подсветка красным и сообщение "Пароль должен содержать цифры и буквы латинского алфавита в верхнем и нижнем регистре"
     - Поле "Подтверждение пароля" (обязательно):
       - Должно совпадать с полем "Новый пароль"
       - Независимая кнопка "Показать/скрыть пароль"
       - Клиентская валидация: если пароли не совпадают - подсветка красным и сообщение "Пароли не совпадают"
     - Кнопка "Установить новый пароль" (активна только при заполнении обоих полей и прохождении валидации)

   - При нажатии кнопки:
     - Проверка валидности токена: не просрочен (≤1 час), не использован ранее (`isUsed = false`)
     - Валидация пароля по требованиям (повторная серверная проверка)
     - Если всё успешно:
       - Обновление хэша пароля в базе данных
       - Установка `isUsed = true` для токена восстановления
       - Инвалидация всех активных сессий пользователя (выход со всех устройств)
       - Отправка email-уведомления: "Пароль для вашего аккаунта был изменён"
       - Перенаправление на страницу входа (`/login`) с сообщением "Пароль успешно изменён. Войдите с новым паролем"

   - Если токен невалиден:
     - Показывается сообщение: "Ссылка восстановления недействительна или просрочена. Запросите новую ссылку"
     - Кнопка "Запросить новую ссылку" (ведёт обратно на `/password/reset`)

Защитные меры:
    - Токен восстановления можно использовать только один раз
    - После успешной смены пароля все предыдущие сессии пользователя сбрасываются
    - При 3 неудачных попытках ввода нового пароля токен блокируется (устанавливается `isUsed = true`)
    - Все запросы на восстановление логируются с указанием:
        - Email пользователя
        - IP-адрес
        - Время запроса
        - Статус (успешно/неудача/причина)
    - Супер-Админ видит все логи восстановления паролей

Особые случаи:
    1. Пользователь не получил письмо:
        - На странице `/password/reset` после отправки: "Если вы не получили письмо, проверьте папку 'Спам' или запросите новую ссылку через 5 минут"
        - Кнопка "Отправить повторно" (доступна через 5 минут после предыдущего запроса)

    2. Множественные запросы:
        - Если пользователь запросил новую ссылку, старая ссылка становится недействительной
        - Действует только последний отправленный токен

    3. Попытка восстановления при активной сессии:
        - Если пользователь уже авторизован и пытается восстановить пароль - перенаправление на страницу смены пароля в профиле (`/profile/change-password`)
        - Сообщение: "Вы уже авторизованы. Для смены пароля перейдите в настройки профиля"
```

#### 3.1.8 Управление доступом пользователей

```
Система предоставляет разные уровни управления доступом пользователей в зависимости от роли:

────────────────────────────────────────────
1. УПРАВЛЕНИЕ ДЛЯ РУКОВОДИТЕЛЯ (в рамках своего проекта)
────────────────────────────────────────────

Руководитель может управлять пользователями ТОЛЬКО в своих проектах:

1. Деактивация в проекте:
   - В разделе "Участники проекта" → выбор пользователя → "Деактивировать в проекте"
   - Предупреждение: "Пользователь потеряет доступ к этому проекту, но сохранит доступ к системе и другим проектам"
   - После деактивации:
     * Пользователь удаляется из активных участников проекта
     * Устанавливается `isActiveInProject = false`
     * Сохраняется вся история пользователя в проекте (задачи, комментарии)
     * Пользователь получает уведомление: "Ваш доступ к проекту «{projectName}» отозван"

2. Активация в проекте (восстановление):
   - В разделе "Неактивные участники" проекта
   - Выбор пользователя → "Восстановить доступ"
   - Пользователь снова становится активным участником проекта
   - Отправляется уведомление: "Ваш доступ к проекту «{projectName}» восстановлен"
   - Все предыдущие задачи и активность сохраняются

3. Удаление из проекта:
   - В разделе "Участники проекта" → выбор пользователя → "Удалить из проекта"
   - Предупреждение с выбором:
     * "Сохранить историю" (рекомендуется):
       - Данные пользователя остаются в истории проекта
       - Отображается как "Удалённый пользователь"
       - Можно пригласить заново
     * "Удалить полностью":
       - Все данные пользователя удаляются из проекта
       - Задачи перепривязываются к "Удалённый пользователь"
       - Необратимая операция
   - Требуется подтверждение: ввод текста "УДАЛИТЬ"
   - После удаления:
     * Пользователь исключается из проекта
     * Может быть приглашён повторно

Ограничения Руководителя:
• Не может деактивировать/удалять других Руководителей проекта (только Системный Админ)
• Не может управлять пользователями вне своих проектов

────────────────────────────────────────────
2. УПРАВЛЕНИЕ ДЛЯ СИСТЕМНОГО АДМИНА (глобальное)
────────────────────────────────────────────

Системный Админ может управлять всеми пользователями:

1. Глобальная деактивация:
   - В админ-панели (/admin/users) → выбор пользователя → "Деактивировать"
   - Предупреждение: "Пользователь потеряет доступ ко всей системе и всем проектам"
   - После деактивации:
     * `isActive = false`
     * Все активные сессии инвалидируются
     * Пользователь удаляется из всех проектов
     * Все данные сохраняются

2. Глобальная активация (восстановление):
   - В разделе "Деактивированные пользователи"
   - Выбор пользователя → "Активировать"
   - Пользователь получает доступ ко всем проектам, в которых состоял
   - Требуется повторное подтверждение email

3. Удаление из системы (полное):
   - В админ-панели → выбор пользователя → "Удалить из системы"
   - Многоступенчатое подтверждение:
     1. Предупреждение: "Все данные пользователя будут удалены безвозвратно"
     2. Проверка: пользователь не должен быть активным в проектах
     3. Ввод текста "УДАЛИТЬ НАВСЕГДА"
     4. Подтверждение паролем Системного Админа
   - После удаления:
     * Все личные данные пользователя удаляются из базы
     * Задачи и комментарии анонимизируются ("Удалённый пользователь")
     * Логи активности сохраняются (для аудита)
     * Необратимая операция

4. Управление в проектах:
    - Системный Админ может выполнять все те же действия, что и Руководитель в любом проекте
    - Может деактивировать/удалять Руководителей в проектах
    - Может назначать новых Руководителей

────────────────────────────────────────────
3. УПРАВЛЕНИЕ ДЛЯ ПОЛЬЗОВАТЕЛЯ (самостоятельное)
────────────────────────────────────────────

1. Удаление своего аккаунта:
    - В настройках профиля (/profile/settings) → "Удалить аккаунт"
      - Процесс удаления:
        1. Многоступенчатое подтверждение:
           - Предупреждение: "Внимание! Это действие необратимо:"
           - "• Все ваши личные данные будут удалены"
           - "• Вы потеряете доступ ко всем проектам"
           - "• Ваши задачи и комментарии будут анонимизированы"
        2. Требуется ввод текущего пароля
        3. Ввод подтверждающего текста "УДАЛИТЬ МОЙ АККАУНТ"
        4. Чекбокс: "Я понимаю, что это действие необратимо"

    - После подтверждения:
        * Устанавливается период ожидания 24 часа: `isMarkedForDeletion = true`, `deletionDate = now + 24h`
        * Пользователь теряет доступ ко всем проектам
        * Отправляется email с подтверждением и ссылкой для отмены
        * В течение 24 часов пользователь может отменить удаление

    - Через 24 часа:
        * Аккаунт автоматически удаляется
        * Все личные данные удаляются
        * Задачи и комментарии помечаются как "Удалённый пользователь"
        * Отправляется финальное уведомление об удалении

2. Отмена удаления (в течение 24 часов):
        - По ссылке из письма-подтверждения
        - Или в настройках профиля (если пользователь ещё может войти)
        - Аккаунт полностью восстанавливается
          * `isMarkedForDeletion = false`
          * Восстанавливается доступ ко всем проектам

────────────────────────────────────────────
ОБЩИЕ ПРАВИЛА И ОГРАНИЧЕНИЯ
────────────────────────────────────────────

1. Защита от случайных действий:
   - Все операции удаления требуют многоступенчатого подтверждения
   - По умолчанию предлагается деактивация вместо удаления
   - Для необратимых операций требуется ввод подтверждающего текста

2. Логирование:
   - Все операции логируются с указанием: кто, кого, когда, тип операции
   - Для удалений дополнительно сохраняется причина (если указана)
   - Логи доступны:
      * Системному Админу: все логи (глобальные логи системы)
      * Руководителю: логи действий пользователей в его проектах
      * Пользователю: доступ к системным логам не предоставляется (только история своих действий через интерфейс)

3. Уведомления:
   - При деактивации/удалении пользователь получает email с информацией
   - При восстановлении/активации — уведомление о восстановлении доступа
   - Руководители проекта уведомляются о значимых изменениях в составе участников

4. Особые случаи:
   - Единственный Руководитель в проекте не может быть деактивирован/удалён без назначения замены
   - Системный Админ не может удалить самого себя
   - Приглашённые пользователи (без пароля) удаляются автоматически через 7 дней
   - Данные удалённых пользователей хранятся в архиве 30 дней для возможного восстановления (только Системным Админом)
```

#### 3.1.9 Автоматический логаут

```
Система автоматически завершает сессии пользователей в следующих случаях:

1. Истечение срока действия сессии:
        - Без "Запомнить меня": 24 часа неактивности
        - С "Запомнить меня": 30 дней неактивности
        - После истечения срока пользователь перенаправляется на `/login` с сообщением "Сессия истекла"

2. Изменение пароля:
        - При смене пароля через восстановление или в настройках профиля
        - Все активные сессии на других устройствах инвалидируются
        - Пользователь получает уведомление: "Ваш пароль был изменён. Все другие сессии завершены"

3. Деактивация аккаунта:
        - При деактивации (самостоятельной или администратором)
        - Все активные сессии немедленно завершаются

4. Подозрительная активность:
        - При входе с нового устройства/местоположения
        - Система может запросить подтверждение через email
        - При 5 неудачных попытках входа с разных IP за короткое время

Техническая реализация:
    - JWT токены с ограниченным сроком действия
    - Refresh tokens для продления сессий
    - Хранение списка активных сессий в базе данных
    - Возможность просмотра и завершения сессий в настройках профиля

Уведомления:
    - При автоматическом завершении сессии пользователь видит соответствующее сообщение
    - При входе с нового устройства отправляется email-уведомление
    - Пользователь может просмотреть все активные сессии в настройках безопасности
```

### 3.2 🏗️ Управление проектами

```mermaid

```

### 3.3 📋 Канбан-доска и задачи

```mermaid

```

### 3.4 👥 Управление группами

```mermaid

```

### 3.5 👤 Управление пользователями

```mermaid

```

### 3.6 📊 Дашборд

```mermaid

```

### 3.7 🔔 Уведомления

```

```

### 3.8 💬 Переписка и комментарии

```

```

### 3.9 📝 Логирование и история

```

```

### 3.10 Профиль пользователя и настройки

```

```

## 4. Нефункциональные требования

### 4.1 ⚡ Производительность

```

```

### 4.2 🔒 Безопасность

```

```

### 4.3 📈 Масштабируемость

```

```

### 4.4 📱 Usability

```


```
