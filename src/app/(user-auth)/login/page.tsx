// src/app/(user-auth)/login/page.tsx

'use client'; // КЛИЕНТСКИЙ КОМПОНЕНТ - выполняется в браузере

import { useState } from 'react'; // Хук для управления состоянием
import { signIn } from 'next-auth/react'; // Функция входа в систему
import { useRouter } from 'next/navigation'; // Навигация между страницами
import Link from 'next/link'; // Компонент для ссылок

export default function LoginPage() {
  // СОСТОЯНИЕ КОМПОНЕНТА:
  // 1. Email пользователя
  const [email, setEmail] = useState('superadmin@workflow.com');
  // 2. Пароль пользователя
  const [password, setPassword] = useState('demo123');
  // 3. Флаг загрузки (true когда идет процесс входа)
  const [isLoading, setIsLoading] = useState(false);
  // 4. Текст ошибки (если вход не удался)
  const [error, setError] = useState('');
  // 5. Хук для навигации по страницам
  const router = useRouter();

  // ОБРАБОТЧИК ОТПРАВКИ ФОРМЫ
  const handleSubmit = async (e: React.FormEvent) => {
    // 1. Блокируем стандартное поведение формы (перезагрузку страницы)
    e.preventDefault();

    // 2. Включаем состояние загрузки и очищаем ошибки
    setIsLoading(true);
    setError('');

    // 3. Пытаемся выполнить вход
    try {
      // Вызываем NextAuth для входа с email и паролем
      const result = await signIn('credentials', {
        email, // Введенный email
        password, // Введенный пароль
        redirect: false, // Не перенаправлять автоматически
      });

      // 4. Проверяем результат
      if (result?.error) {
        // Если есть ошибка - показываем сообщение
        setError('Неверный email или пароль');
      } else {
        // Если успешно - переходим на выбор проекта
        router.push('/project-select');
      }
    } catch (error) {
      // Ловим непредвиденные ошибки
      setError('Произошла ошибка при входе');
    } finally {
      // Всегда выключаем индикатор загрузки
      setIsLoading(false);
    }
  };

  // РАЗМЕТКА КОМПОНЕНТА
  return (
    <div className="bg-surface rounded-lg shadow-sm p-8">
      {/* ЗАГОЛОВОК */}
      <div className="text-center mb-8">
        <h2 className="text-2xl font-bold text-text-primary">Вход в систему</h2>
        <p className="text-text-secondary mt-2">Введите свои учетные данные для входа</p>
      </div>

      {/* ФОРМА ВХОДА */}
      <form onSubmit={handleSubmit} className="space-y-6">
        {/* БЛОК ОШИБКИ (показывается только если error не пустой) */}
        {error && (
          <div className="bg-error/10 border border-error/20 text-error px-4 py-3 rounded-lg text-sm">
            {error}
          </div>
        )}

        {/* ПОЛЕ EMAIL */}
        <div>
          <label htmlFor="email" className="block text-sm font-medium text-text-secondary mb-2">
            Email
          </label>
          <input
            id="email"
            type="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)} // Обновляем состояние при вводе
            className="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-colors"
            placeholder="your@email.com"
            required
            disabled={isLoading} // Отключаем поле во время загрузки
          />
        </div>

        {/* ПОЛЕ ПАРОЛЯ */}
        <div>
          <label htmlFor="password" className="block text-sm font-medium text-text-secondary mb-2">
            Пароль
          </label>
          <input
            id="password"
            type="password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            className="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-colors"
            placeholder="Ваш пароль"
            required
            disabled={isLoading}
          />
        </div>

        {/* КНОПКА ВХОДА */}
        <button
          type="submit"
          disabled={isLoading}
          className="w-full bg-primary text-white py-3 px-4 rounded-lg font-semibold hover:bg-primary-dark transition-colors disabled:opacity-50 disabled:cursor-not-allowed">
          {isLoading ? (
            // Индикатор загрузки (крутящийся спиннер)
            <div className="flex items-center justify-center">
              <div className="w-5 h-5 border-2 border-white border-t-transparent rounded-full animate-spin mr-2"></div>
              Вход...
            </div>
          ) : (
            // Обычный текст кнопки
            'Войти'
          )}
        </button>
      </form>

      {/* ДЕМО ДОСТУПЫ ДЛЯ ТЕСТИРОВАНИЯ */}
      <div className="mt-6 p-4 bg-gray-50 rounded-lg">
        <h3 className="text-sm font-medium text-text-secondary mb-3">Демо доступы:</h3>

        {/* 👑 СУПЕР-АДМИН */}
        <div className="mb-3 pb-2 border-b border-gray-200">
          <div className="text-xs font-medium text-purple-600 mb-1">
            👑 СУПЕР-АДМИН (все проекты)
          </div>
          <div className="text-xs">superadmin@workflow.com / demo123</div>
        </div>

        {/* 👨‍💼 АДМИНЫ ПРОЕКТОВ */}
        <div className="mb-3 pb-2 border-b border-gray-200">
          <div className="text-xs font-medium text-blue-600 mb-1">👨‍💼 АДМИНЫ ПРОЕКТОВ</div>
          <div className="space-y-1 text-xs">
            <div>
              <strong>Ольга (Успешный бизнес):</strong> admin.olya@workflow.com / demo123
            </div>
            <div>
              <strong>Славка (Начинающий бизнес):</strong> admin.slava@workflow.com / demo123
            </div>
          </div>
        </div>

        {/* 👥 СОТРУДНИКИ - УСПЕШНЫЙ БИЗНЕС */}
        <div className="mb-3 pb-2 border-b border-gray-200">
          <div className="text-xs font-medium text-green-600 mb-1">👥 УСПЕШНЫЙ БИЗНЕС (Ольга)</div>
          <div className="space-y-1 text-xs">
            <div>
              <strong>ОЗОН - Алена:</strong> bussiness.manager@wf.com / demo123
            </div>
            <div>
              <strong>ОЗОН - Лена:</strong> bussiness.manager2@wf.com / demo123
            </div>
            <div>
              <strong>WB - Аня:</strong> bussiness.manager3@wf.com / demo123
            </div>
            <div>
              <strong>WB - Боно:</strong> bussiness.manager4@wf.com / demo123
            </div>
          </div>
        </div>

        {/* 👥 СОТРУДНИКИ - НАЧИНАЮЩИЙ БИЗНЕС */}
        <div className="mb-3 pb-2 border-b border-gray-200">
          <div className="text-xs font-medium text-orange-600 mb-1">
            👥 НАЧИНАЮЩИЙ БИЗНЕС (Славка)
          </div>
          <div className="space-y-1 text-xs">
            <div>
              <strong>Маша:</strong> bussiness2.manager1@wf.com / demo123
            </div>
            <div>
              <strong>Миша:</strong> bussiness2.manager2@wf.com / demo123
            </div>
            <div>
              <strong>Саша:</strong> bussiness2.manager3@wf.com / demo123
            </div>
          </div>
        </div>

        {/* 👥 СОТРУДНИКИ - ДОМАШНИЕ ДЕЛА */}
        <div className="mb-2">
          <div className="text-xs font-medium text-pink-600 mb-1">👥 ДОМАШНИЕ ДЕЛА</div>
          <div className="space-y-1 text-xs">
            <div>
              <strong>Викуша:</strong> super.devochka@wf.com / demo123
            </div>
          </div>
        </div>

        {/* 💡 ПОДСКАЗКИ */}
        <div className="mt-3 pt-3 border-t border-gray-200">
          <h4 className="text-xs font-medium text-text-secondary mb-1">💡 Особенности системы:</h4>
          <div className="text-xs text-gray-500 space-y-0.5">
            <div>
              • <strong>Супер-админ</strong> видит ВСЕ проекты
            </div>
            <div>
              • <strong>Админы</strong> видят только свои проекты
            </div>
            <div>
              • <strong>Сотрудники</strong> видят только назначенные проекты
            </div>
            <div>• После входа выберите проект для работы</div>
          </div>
        </div>
      </div>
    </div>
  );
}

//----------------------------------------------------------------------------
// // src/app/(auth)/login/page.tsx

// /**
//  * 🎯 СТРАНИЦА ВХОДА В DIRECTWORKFLOW
//  *
//  * Этот файл - страница входа в систему, доступная по URL: /auth/login
//  * Пользователь вводит email и пароль, система проверяет credentials и создает сессию
//  *
//  * 📁 РАСПОЛОЖЕНИЕ:
//  * src/app/(auth)/login/page.tsx
//  *
//  * 🎪 КАК РАБОТАЕТ NEXT.JS:
//  * - Next.js автоматически создает маршрут /auth/login из этой структуры папок
//  * - Страница использует AuthLayout из (auth)/layout.tsx
//  * - Это Client Component потому что использует React состояния и хуки
//  */

// /**
//  * ⚡ 'use client' - ДИРЕКТИВА ДЛЯ CLIENT COMPONENT
//  *
//  * Эта директива говорит Next.js что этот компонент:
//  * - 🎯 Выполняется на КЛИЕНТЕ (в браузере)
//  * - ✅ Может использовать React хуки (useState, useEffect)
//  * - ✅ Может обрабатывать пользовательские события (onClick, onChange)
//  * - ✅ Может использовать браузерные API
//  * - ❌ НЕ может использовать Server Components features
//  *
//  * Без 'use client' компонент был бы Server Component по умолчанию
//  */
// 'use client';

// // =============================================================================
// // ИМПОРТЫ - ЧТО ИЗ ЧЕГО ИМПОРТИРУЕМ
// // =============================================================================

// /**
//  * ⚛️ React хуки для управления состоянием компонента
//  * - useState: для хранения и обновления состояния
//  */
// import { useState } from 'react';

// /**
//  * 🔐 NextAuth функции для аутентификации
//  * - signIn: функция для входа пользователя
//  */
// import { signIn } from 'next-auth/react';

// /**
//  * 🧭 Next.js навигация
//  * - useRouter: хук для программной навигации между страницами
//  */
// import { useRouter } from 'next/navigation';

// /**
//  * 🔗 Next.js компонент для клиентской навигации
//  * - Link: оптимизированная замена <a> тега для SPA-переходов
//  */
// import Link from 'next/link';

// // =============================================================================
// // КОМПОНЕНТ СТРАНИЦЫ ВХОДА
// // =============================================================================

// /**
//  * 🎪 LoginPage - Основной компонент страницы входа
//  *
//  * Этот компонент:
//  * - 📝 Отображает форму входа
//  * - 🔐 Обрабатывает аутентификацию через NextAuth
//  * - 🎨 Показывает состояния загрузки и ошибки
//  * - 🧭 Перенаправляет после успешного входа
//  */
// export default function LoginPage() {
//   // ===========================================================================
//   // СОСТОЯНИЯ КОМПОНЕНТА (useState hooks)
//   // ===========================================================================

//   /**
//    * 📧 Состояние для email пользователя
//    * - email: текущее значение поля ввода
//    * - setEmail: функция для обновления значения
//    */
//   const [email, setEmail] = useState('');

//   /**
//    * 🔐 Состояние для пароля пользователя
//    * - password: текущее значение поля пароля
//    * - setPassword: функция для обновления значения
//    */
//   const [password, setPassword] = useState('');

//   /**
//    * ⏳ Состояние загрузки (показывает идет ли процесс входа)
//    * - isLoading: true когда выполняется запрос аутентификации
//    * - setIsLoading: функция для изменения состояния загрузки
//    */
//   const [isLoading, setIsLoading] = useState(false);

//   /**
//    * ❌ Состояние ошибки (хранит текст ошибки если вход не удался)
//    * - error: строка с сообщением об ошибке или пустая строка если ошибок нет
//    * - setError: функция для установки/очистки ошибки
//    */
//   const [error, setError] = useState('');

//   /**
//    * 🧭 Хук useRouter для программной навигации
//    * - router: объект с методами для перехода между страницами
//    * - push(): метод для перехода на указанный URL
//    */
//   const router = useRouter();

//   // ===========================================================================
//   // ОБРАБОТЧИК ФОРМЫ ВХОДА
//   // ===========================================================================

//   /**
//    * 🎯 handleSubmit - обработчик отправки формы входа
//    *
//    * Этот асинхронный метод:
//    * 1. 🛑 Предотвращает стандартное поведение формы (перезагрузку страницы)
//    * 2. ⏳ Устанавливает состояние загрузки
//    * 3. 🧹 Очищает предыдущие ошибки
//    * 4. 🔐 Вызывает NextAuth signIn для аутентификации
//    * 5. ✅ При успехе - перенаправляет на страницу выбора проекта
//    * 6. ❌ При ошибке - показывает сообщение
//    * 7. 🧹 В любом случае сбрасывает состояние загрузки
//    *
//    * @param e - событие формы React (React.FormEvent)
//    */
//   const handleSubmit = async (e: React.FormEvent) => {
//     // 1. 🛑 Предотвращаем стандартную отправку формы (перезагрузку страницы)
//     e.preventDefault();

//     // 2. ⏳ Включаем состояние загрузки - показываем пользователю что идет процесс
//     setIsLoading(true);

//     // 3. 🧹 Очищаем предыдущие ошибки (если были)
//     setError('');

//     try {
//       /**
//        * 4. 🔐 ВЫЗОВ NEXTAUTH SIGNIN
//        *
//        * signIn('credentials', ...) отправляет запрос к нашему API:
//        * POST /api/auth/callback/credentials
//        *
//        * Параметры:
//        * - 'credentials': используем провайдер credentials (логин/пароль)
//        * - email, password: данные из формы
//        * - redirect: false - не перенаправлять автоматически, хотим обработать результат
//        *
//        * NextAuth вызывает нашу функцию authorize() из auth.ts которая:
//        * - Ищет пользователя в БД по email
//        * - Проверяет пароль (в нашем случае сравнивает с 'demo123')
//        * - Возвращает объект пользователя или null
//        */
//       const result = await signIn('credentials', {
//         email,        // Email из состояния компонента
//         password,     // Пароль из состояния компонента
//         redirect: false, // Не перенаправлять автоматически
//       });

//       /**
//        * 5. 🔍 ПРОВЕРКА РЕЗУЛЬТАТА АУТЕНТИФИКАЦИИ
//        *
//        * result объект содержит:
//        * - error: строка с ошибкой или undefined если успех
//        * - url: URL для перенаправления или undefined
//        * - status: HTTP статус
//        */
//       if (result?.error) {
//         // ❌ ЕСТЬ ОШИБКА - показываем сообщение пользователю
//         setError('Неверный email или пароль');
//       } else {
//         // ✅ УСПЕШНЫЙ ВХОД - перенаправляем на страницу выбора проекта
//         router.push('/project-select');
//       }
//     } catch (error) {
//       // 🚨 НЕПРЕДВИДЕННАЯ ОШИБКА (например, проблемы с сетью)
//       setError('Произошла ошибка при входе');
//     } finally {
//       // 7. 🧹 В ЛЮБОМ СЛУЧАЕ выключаем состояние загрузки
//       setIsLoading(false);
//     }
//   };

//   // ===========================================================================
//   // RENDER - ОТРИСОВКА КОМПОНЕНТА
//   // ===========================================================================

//   /**
//    * 🎨 Возвращаем JSX разметку страницы входа
//    *
//    * Эта разметка:
//    * - Использует наши кастомные CSS классы (primary, surface, text-primary и т.д.)
//    * - Адаптивна и доступна
//    * - Показывает состояния загрузки и ошибки
//    */
//   return (
//     /**
//      * 📦 ОСНОВНОЙ КОНТЕЙНЕР ФОРМЫ
//      *
//      * Стили:
//      * - bg-surface: фон карточки (белый или другой из нашей темы)
//      * - rounded-lg: скругленные углы
//      * - shadow-sm: легкая тень для эффекта глубины
//      * - p-8: большие отступы внутри (32px)
//      */
//     <div className="bg-surface rounded-lg shadow-sm p-8">

//       {/* 🏷️ ЗАГОЛОВОК ФОРМЫ */}
//       <div className="text-center mb-8">
//         <h2 className="text-2xl font-bold text-text-primary">
//           Вход в систему
//         </h2>
//         <p className="text-text-secondary mt-2">
//           Введите свои учетные данные для входа
//         </p>
//       </div>

//       {/* 📝 ФОРМА ВХОДА */}
//       <form onSubmit={handleSubmit} className="space-y-6">

//         {/* ❌ БЛОК ОШИБКИ (показывается только если есть ошибка) */}
//         {error && (
//           <div className="bg-error/10 border border-error/20 text-error px-4 py-3 rounded-lg text-sm">
//             {error}
//           </div>
//         )}

//         {/* 📧 ПОЛЕ EMAIL */}
//         <div>
//           <label htmlFor="email" className="block text-sm font-medium text-text-secondary mb-2">
//             Email
//           </label>
//           <input
//             id="email"
//             type="email"
//             value={email}
//             onChange={(e) => setEmail(e.target.value)}
//             className="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-colors"
//             placeholder="your@email.com"
//             required
//             disabled={isLoading}
//           />
//         </div>

//         {/* 🔐 ПОЛЕ ПАРОЛЯ */}
//         <div>
//           <label htmlFor="password" className="block text-sm font-medium text-text-secondary mb-2">
//             Пароль
//           </label>
//           <input
//             id="password"
//             type="password"
//             value={password}
//             onChange={(e) => setPassword(e.target.value)}
//             className="w-full px-4 py-3 border border-gray-200 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-colors"
//             placeholder="Ваш пароль"
//             required
//             disabled={isLoading}
//           />
//         </div>

//         {/* 🎯 КНОПКА ВХОДА */}
//         <button
//           type="submit"
//           disabled={isLoading}
//           className="w-full bg-primary text-white py-3 px-4 rounded-lg font-semibold hover:bg-primary-dark transition-colors disabled:opacity-50 disabled:cursor-not-allowed">
//           {isLoading ? (
//             // ⏳ СОСТОЯНИЕ ЗАГРУЗКИ - показываем спиннер
//             <div className="flex items-center justify-center">
//               <div className="w-5 h-5 border-2 border-white border-t-transparent rounded-full animate-spin mr-2"></div>
//               Вход...
//             </div>
//           ) : (
//             // 📝 ОБЫЧНОЕ СОСТОЯНИЕ - текст "Войти"
//             'Войти'
//           )}
//         </button>
//       </form>

//       {/* ℹ️ БЛОК С ДЕМО ДОСТУПАМИ */}
//       <div className="mt-6 p-4 bg-gray-50 rounded-lg">
//         <h3 className="text-sm font-medium text-text-secondary mb-2">Демо доступы:</h3>
//         <div className="space-y-1 text-xs">
//           <div><strong>Супер-админ:</strong> superadmin@workflow.com / demo123</div>
//           <div><strong>Админ (Электронная коммерция):</strong> admin.ecommerce@workflow.com / demo123</div>
//           <div><strong>Сотрудник (ОЗОН):</strong> ozon.manager@workflow.com / demo123</div>
//         </div>
//       </div>
//     </div>
//   );
// }

// /**
//  * 🎪 ПОЛНЫЙ FLOW РАБОТЫ СТРАНИЦЫ:
//  *
//  * 1. 📍 ПОЛЬЗОВАТЕЛЬ ПЕРЕХОДИТ НА /auth/login
//  * 2. 🎨 RENDER: React отрисовывает форму входа
//  * 3. 📝 ПОЛЬЗОВАТЕЛЬ ЗАПОЛНЯЕТ ФОРМУ:
//  *    - Вводит email → setEmail() обновляет состояние
//  *    - Вводит пароль → setPassword() обновляет состояние
//  * 4. 🎯 ПОЛЬЗОВАТЕЛЬ НАЖИМАЕТ "ВОЙТИ":
//  *    - Вызывается handleSubmit
//  *    - setIsLoading(true) - показываем спиннер
//  * 5. 🔐 АУТЕНТИФИКАЦИЯ:
//  *    - signIn() отправляет запрос к /api/auth/callback/credentials
//  *    - NextAuth вызывает нашу authorize() функцию из auth.ts
//  *    - authorize() ищет пользователя в БД и проверяет пароль
//  * 6. ✅/❌ ОБРАБОТКА РЕЗУЛЬТАТА:
//  *    - УСПЕХ: router.push('/project-select') - перенаправляем
//  *    - ОШИБКА: setError('...') - показываем ошибку
//  * 7. 🧹 ЗАВЕРШЕНИЕ: setIsLoading(false) - убираем спиннер
//  */

// /**
//  * 🛡️ БЕЗОПАСНОСТЬ И ВАЛИДАЦИЯ:
//  *
//  * - ✅ type="email" - браузерная валидация email
//  * - ✅ required - обязательные поля
//  * - ✅ disabled при загрузке - защита от повторных отправок
//  * - ✅ Пароль скрыт type="password"
//  * - ❌ Пароли в демо-доступах видны (только для демо!)
//  */

// /**
//  * 🎨 ДИЗАЙН И UX:
//  *
//  * - ✅ Индикатор загрузки (спиннер)
//  * - ✅ Сообщения об ошибках
//  * - ✅ Плавные переходы (transition-colors)
//  * - ✅ Фокус-стили (focus:ring-2)
//  * - ✅ Демо-данные для тестирования
//  * - ✅ Адаптивная верстка
//  */

// /**
//  * 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ:
//  *
//  * - Client Component ('use client')
//  * - Использует NextAuth для аутентификации
//  * - Использует App Router (next/navigation)
//  * - TypeScript для типобезопасности
//  * - Tailwind CSS для стилизации
//  */
